name: CI

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  core-tests:
    name: Linux LocalCoderCore
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Swift
        uses: swift-actions/setup-swift@v2
        with:
          swift-version: "6.2"

      - name: Print toolchain
        run: |
          uname -a
          swift --version

      - name: Build LocalCoderCore only
        run: swift build --target LocalCoderCore

      - name: Test LocalCoderCore only
        timeout-minutes: 10
        run: swift test --filter LocalCoderCoreTests
