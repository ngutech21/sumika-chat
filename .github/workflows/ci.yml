name: CI

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Static analysis
    runs-on: macos-26
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install lint tools
        timeout-minutes: 10
        run: brew install just swiftlint swift-format


      - name: Run format
        timeout-minutes: 10
        run: just format

      - name: Run SwiftLint
        timeout-minutes: 10
        run: just lint
    

  unit-tests:
    name: macOS Core, App, and MLX unit tests
    runs-on: macos-26
    timeout-minutes: 45
    env:
      CLONED_SOURCE_PACKAGES_DIR_PATH: .build/xcode-source-packages
      # Hosted runners cannot approve mlx-swift package plugins or macros interactively.
      SKIP_PACKAGE_PLUGIN_VALIDATION: "1"

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
    
      - name: Install lint tools
        timeout-minutes: 10
        run: brew install just

      - name: Setup Swift
        uses: swift-actions/setup-swift@364295d9c23900ce04d4e5cc708387921b4e50f9 # v3.0.0-beta.1
        with:
          swift-version: "6.3.2"
          skip-verify-signature: true

      - name: Cache SwiftPM dependencies
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            .build/xcode-source-packages
            .build/repositories
            .build/checkouts
            .build/artifacts
          key: ${{ runner.os }}-swift-6.3.2-swiftpm-${{ hashFiles('Package.resolved', 'Sumika.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved') }}

      - name: Print toolchain
        run: |
          uname -a
          swift --version

      - name: Verify Swift package lockfiles
        timeout-minutes: 10
        run: just check-package-locks

      - name: Run unit tests
        timeout-minutes: 30
        run: just test
